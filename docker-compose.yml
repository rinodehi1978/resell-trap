services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - DATABASE_URL=sqlite:////data/resell_trap.db
    volumes:
      - app-data:/data
    restart: unless-stopped

volumes:
  app-data:
